# Port the streaming server listens on
PORT=8080

# Shared-secret token required to access the video stream (and any future
# control endpoints). Enforced — the server refuses to start without it.
# Generate your own value and put it in backend/.env (gitignored) — never
# commit a real one.
STREAM_AUTH_TOKEN=changeme
